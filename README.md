# earthquake-data-analysis
A Python-based data extraction tool that fetches live earthquake records from the USGS API, processes the data using Pandas, and saves it into a structured CSV format for further analysis
